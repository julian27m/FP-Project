functor
import 
    System(showInfo:Show)

export 
    Parse
    Saludo

define
    Saludo = "The Parser module is working correctly."

    fun {Parse ProgramaTexto}
        "GrafoSimulado(Parser)"
    end
end
